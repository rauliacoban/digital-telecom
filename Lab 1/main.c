/*
main:
{
    open file
    while(!EOF)
    {
        read char   
        parser(char)
        if( ready==ok)
            print data
        if(ready = error)
            stop and print error message
            "expected %c and got %c"
    
    }
}
*/